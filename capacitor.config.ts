import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.maintainify.app',
  appName: 'Maintainify',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
